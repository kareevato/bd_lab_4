# Prometheus Full Flask Backend (15 tables)
1) Run SQL: `sql/01_init_prometheus.sql` (creates DB/user + tables + seed).
2) Copy `.env.example` → `.env`.
3) Create venv, install `requirements.txt`.
4) `python run.py` and test via Postman.
